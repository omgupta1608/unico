# unico
A simple unit conversion tool written in Rust.


## Usage
(add ss in between steps)
- Run `unico <value>`
- Select the type of conversion
- Select `from` unit
- Select `to` unit
- Get the result